# Claude Code 기본 사용법 & 워크플로우 꿀팁
*(발표 시간: 약 10~20분 / 참고: PyTorch KR 포럼 "Claude Code Tips" 글, 원본 GitHub: ykdojo/claude-code-tips)*

---

## 1. Claude Code가 뭔가요? (2분)

- Anthropic이 만든 **터미널 기반 AI 코딩 에이전트**
- 자연어로 지시하면 알아서 **파일 탐색 → 코드 작성/수정 → 테스트 → Git 관리**까지 수행
- 핵심 차별점: IDE 플러그인이 아니라 **터미널 자체를 통합 개발 환경으로 씀**
  - 파일 찾기 → 코드 수정 → 테스트 실행 → 결과 해석까지 끊김 없이 하나의 흐름으로 처리

---

## 2. 시작하기 (3분)

- 실행: 프로젝트 폴더에서 `claude` 입력
- 대화형으로 요청하면 됨 — "이 함수 버그 고쳐줘", "테스트 추가해줘" 등
- **슬래시 커맨드** 몇 가지만 알아도 충분:
  - `/clear` — 현재 세션(대화 맥락) 초기화
  - `/compact` — 지금까지 대화를 요약해서 압축
  - `/help` — 도움말

- **터미널 별칭 설정해두면 편함**
  - 예: `c='claude'` 로 alias 걸어두면 매번 `claude` 안 쳐도 됨

---

## 3. 기본 사용 팁 (3~4분)

| 팁 | 내용 |
|---|---|
| 절대 경로 사용 | 상대경로 대신 `realpath` 등으로 절대경로 넘겨주면 헷갈릴 일이 없음 |
| 터미널 출력 그대로 붙여넣기 | 에러 로그, 콘솔 출력을 통째로 복사해서 붙여넣어도 됨. 사람 눈엔 깨져 보여도 LLM은 잘 파악함 |
| 음성 입력 활용 | 타이핑 대신 음성 인식 툴(MacWhisper 등)로 빠르게 지시하는 것도 방법 |
| 글쓰기 파트너로도 활용 | 코드뿐 아니라 문서화, 블로그 초안, 이메일 작성에도 사용 가능 |

---

## 4. 워크플로우 관리 — 오늘의 핵심 (7~8분)

이 부분이 실제로 생산성에 가장 크게 영향을 주는 부분입니다.

### 🔑 (1) 문제를 쪼개서 지시하기
- "전체 시스템을 만들어줘" ❌
- "DB 스키마부터 설계해줘" → "그 다음 API 구현해줘" ✅
- 단계별로 지시하면 **성공률이 비약적으로 상승**함
  - 한 번에 너무 큰 작업을 맡기면 중간에 엉뚱한 방향으로 새기 쉬움

### 🔑 (2) 컨텍스트는 신선하게 유지하기
- 주제가 완전히 바뀌면 `/clear`로 세션 초기화하고 새로 시작
- 예전 대화 맥락을 계속 끌고 가면 오히려 답변 품질이 떨어짐
- 관련 없는 이전 작업 히스토리는 노이즈가 됨

### 🔑 (3) 컨텍스트 압축을 적극적으로
- 대화가 길어지면 `/compact`로 압축
- 세션을 넘어가야 할 땐 **핸드오프(인수인계) 문서**를 직접 작성해두기
  - "지금까지 한 작업, 남은 작업, 주의할 점"을 정리한 md 파일 하나 만들어두면
  - 다음 세션에서 그 문서만 던져줘도 맥락이 바로 이어짐

### 🔑 (4) 지난 대화 기록도 검색 가능
- 세션 로그는 `~/.claude/projects/` 경로 아래 `.jsonl` 파일로 저장됨
- `grep`, `find`로 예전에 뭘 물어봤는지, 어떤 해결책을 썼는지 검색 가능
- "저번에 이거 어떻게 고쳤더라?" 할 때 유용

### 🔑 (5) 탭으로 멀티태스킹
- 터미널 탭 여러 개 띄워서 여러 작업을 병렬로 진행
- 한 탭에서 Claude가 작업하는 동안 다른 탭에서 다음 작업 지시
- 유휴 시간(내가 기다리기만 하는 시간)을 최소화하는 방식

### 🔑 (6) "10억 토큰의 법칙"
- 요약: **많이 써봐야 감이 생긴다**
- 아끼지 말고 고성능 모델을 과감히 써보면서
- AI와 어떻게 상호작용해야 잘 되는지 직관을 쌓는 것 자체가 장기적으로 생산성을 높임

---

## 5. Git 연동 팁 (2분, 시간 남으면)

- Git/GitHub CLI 작업(`git`, `gh`)도 자연어로 위임 가능 — 예: "Draft PR 만들어줘"
- **Git worktree**를 활용하면 브랜치별로 작업 공간을 분리해서 병렬 작업 가능
- PR 리뷰도 대화형으로: 테스트 코드 작성 시키고 검증까지 맡기기

---

## 6. 마무리 요약 (1분)

1. **작게 쪼개서 지시할 것** — 한 번에 다 시키지 말기
2. **맥락 관리가 생산성을 좌우함** — `/clear`, `/compact`, 핸드오프 문서
3. **터미널을 IDE처럼 통으로 활용** — 탐색-수정-테스트를 끊김 없이
4. **많이 써보면서 감을 쌓는 게 최선** — 완벽한 프롬프트보다 반복 경험이 중요

> 원본: PyTorch KR 포럼 "[3.0] Claude Code (feat. ykdojo Claude Code Tips)" — 9bow(박정환) 작성, GitHub `ykdojo/claude-code-tips` 기반

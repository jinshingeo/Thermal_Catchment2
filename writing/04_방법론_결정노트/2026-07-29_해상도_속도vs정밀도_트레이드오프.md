작성일: 2026-07-29
버전: v3(링크 버퍼 버그 수정 — 15m 비단조성 해소)
근거논문: 없음(자체 산출 비교 검증 — SOLWEIG 파이프라인 실행 결과 간 비교, 외부 문헌 인용 아님)

## 0-1. v3 업데이트 — 링크 버퍼 버그 발견 및 수정

**버그 경위**: v2까지 링크 단위 정밀도 계산에서 `links.buffer(5.0)`(고정 5m
반경)을 쓰고 `zonal_stats`에 `all_touched` 옵션을 안 줬다. 반면 메인 분석
(`13_assign_tmrt_to_links_5m.py`)은 **각 링크의 실제 도로폭 절반**
(`width_final/2`)을 버퍼로, `all_touched=True`로 zonal mean을 계산한다.
고정 버퍼가 해상도별 픽셀 크기보다 작아지면(15m·30m에서), 버퍼 안에 픽셀
중심점이 안 걸리는 링크가 대량 탈락(all_touched 없이는 "닿기만 해도 포함"이
안 됨) — 그 결과 **해상도별 링크 표본 수 자체가 달랐다**(5m=16316, 10m=16288,
15m=14274, 30m=7792). 15m의 Hard Cut 일치율이 10m·30m보다 낮게 나온
비단조성(v2 §0)이 이 표본 불일치 때문이었을 가능성이 제기되어(사용자 지적),
메인 분석과 동일한 방식(`width_final/2` 버퍼 + `all_touched=True`)으로 전면
재계산했다.

**수정 후 결과**: 5개 해상도(1/5/10/15/30m) **전부 16,316개 링크로 완전히
동일**해졌다. 정밀도 수치도 재계산:

| 해상도 | 소요시간 | 링크 Tmrt r | 링크 UTCI r | 링크 HardCut38 일치 |
|---|---|---|---|---|
| 1m(기준) | 85,999초 | 1.000 | 1.000 | 100.00% |
| 5m | 1,100초 | 0.939 | 0.939 | 98.95% |
| 10m | 231초 | 0.825 | 0.824 | 98.16% |
| 15m | 80초 | 0.731 | 0.729 | 98.38% |
| 30m | 23초 | 0.575 | 0.573 | 98.23% |

- **연속값 상관계수(r)는 이제 5m→10m→15m→30m 순서로 완전히 단조 감소한다**
  (0.939→0.825→0.731→0.575) — v2의 비단조성은 실제 해상도 효과가 아니라
  버퍼 버그로 인한 표본 불일치였음이 확인됨.
- **Hard Cut 일치율은 여전히 98.16~98.95%로 좁은 범위 안에서 오르내린다**
  (완전한 단조는 아니나 변동폭이 1%p 이내로 매우 작음 — 이 정도는 노이즈
  범위로 봐도 무방). 즉 "Hard Cut은 해상도에 매우 강건하다"는 v1의 핵심
  결론은 버그 수정 후에도 그대로 유지된다.
- 소요시간(총 소요시간)은 이 버그와 무관해 변경 없음.

**새 파일**: `03_Method_C/code/regenerate_1mtrue_links_widthbuffer.py`,
`03_Method_C/results/2026-07-29_link_tmrt_utci_seongdong_1mtrue_v2.gpkg`,
`03_Method_C/results/compare_resolution_tradeoff/2026-07-29_precision_vs_1mtrue_v3.csv`,
`2026-07-29_tradeoff_merged_v3.csv`, 그래프 3종(`..._v3.png`,
`plot_resolution_tradeoff_v3.py`). `compute_precision_vs_1mtrue.py`는 버그
수정 후 원본 파일 자체를 업데이트(스크립트는 재사용 목적이라 결과 파일과
달리 새 버전 안 만듦 — 결과는 전부 새 파일로 분리 저장).

**결론(갱신)**: v1~v2의 "5m은 엘보우 그 자체는 아니지만 실행가능선 안에서
가장 정밀하다"는 논지는 버그 수정 후에도 그대로 유지되며, 오히려 연속값
r이 이제 깨끗하게 단조 감소해 논증이 더 명확해졌다. 15m의 이상한 결과는
방법론 버그였음이 확인되어 해소됨.

## 0. v2 업데이트 요약 (사용자 피드백 반영)

- **15m 추가**: 기존 5/10/30m과 동일 방식(1mtrue 다운샘플 소스)으로 15m 실행.
  소요시간 80초(1m 대비 약 1075배 빠름). 정밀도: 링크 UTCI r=0.706,
  Hard Cut38 일치율 **97.58%** — 흥미롭게도 **10m(98.02%)·30m(97.69%)보다
  낮음**(비단조적). 링크 매칭 수(14,274)가 다른 해상도(16,288~16,316)보다
  적어 표본 차이의 영향일 가능성 있음 — 있는 그대로 기록, 추가 조사 필요.
  원자료: `2026-07-29_tradeoff_merged_v2.csv`.
- **그래프 x축**: 30m/15m/10m/5m/1m 순서로 등간격 범주형으로 변경(기존 로그
  스케일 폐기)
- **그래프 y축(소요시간)**: 로그스케일 대신 **물결선 축절단**(70,000~90,000초
  구간 생략)으로 변경, 5m→1m 구간의 급격한 증가를 화살표로 강조
- 새 그래프: `2026-07-29_resolution_tradeoff_speed_vs_precision_v2.png`,
  `2026-07-29_resolution_feasibility_threshold_v2_left.png`(소요시간),
  `..._right.png`(정밀도) — 기존 v1 그래프는 삭제하지 않고 보존
- 코드: `merge_tradeoff_v2.py`, `plot_resolution_tradeoff_v2.py`,
  `compute_precision_vs_1mtrue.py`(15m 케이스 추가로 수정됨)

# 해상도(1/5/10/30m)별 산출 속도 vs 정밀도 트레이드오프

## 1. 목적 — 배경(박진우 교수님 제안)

지도교수님(박진우 교수님)이 제안: 서울 전역 메인 분석에 5m 해상도를 채택한 것이 임의적
선택이 아니라 근거 있는 선택임을 방어할 자료가 필요하다 — 해상도를 올릴수록(격자를
조밀하게 할수록) 정밀도는 개선되지만 계산 비용도 커지는데, 어느 지점부터 "정밀도 개선이
완만해지는가"(엘보우 포인트)를 실제로 산출해 5m 근처에서 그 지점이 나타나는지 보자는
것이었다.

## 2. 방법

**핵심 설계 원칙**: 넷(1m/5m/10m/30m) 모두 **1mtrue(성동구 진짜 1m DEM 소스, 9분할
타일링, 2026-07-23 완료)를 평균/최빈값 다운샘플링한 동일 소스**에서 산출한다. 지형·건물·
수목 데이터가 완전히 동일하고 격자 해상도만 다르므로, 순수한 해상도 효과만 측정하는
가장 깨끗한 비교가 된다(기존에 무효 판정된 `2026-07-28_성동구_5m_1mtrue_비교결과.md`나
서울 클립본 비교 `2026-07-28_성동구_v2v3_1mtrue_비교결과_정정판.md`와 달리, 이번엔 애초에
비교 대상 넷 모두 같은 산출 스크립트·같은 실행일로 통일).

1. 1mtrue의 DSM/CDSM/DEM(연속형, average)·LandCover(범주형, mode)를 5m/10m/30m로
   각각 다운샘플링(`downsample_seongdong_1mtrue.py`)
2. 각 해상도마다 UMEP Wall Height/Aspect → SVF(anisotropic) → SOLWEIG → UTCI
   (`pythermalcomfort`) 전체 파이프라인을 **타일링 없이 단일 실행**(격자가 1m 대비
   1/25~1/900로 작아 메모리 문제 없음 — 사전에 shadowmat 크기 계산 후 진행,
   `run_seongdong_clean_pipeline.sh`)
3. 각 단계(다운샘플/Wall/SVF/SOLWEIG/UTCI) 소요시간을 초 단위로 로그 기록
4. 정밀도: 1mtrue를 기준값(ground truth)으로 삼아, 5m/10m/30m 각각을 1mtrue와
   픽셀 단위(1mtrue를 해당 해상도로 리샘플링 후 비교) 및 링크 단위(도로망 16,316개
   링크, 5m 버퍼+zonal mean, `compare_v2v3_1mtrue_seongdong.py`와 동일 방법)로
   MAE·상관계수 r·Hard Cut(UTCI≥38°C) 일치율 산출(`compute_precision_vs_1mtrue.py`)
5. 1m 자체의 소요시간은 이미 확보된 값을 그대로 사용(재계산 안 함): 9분할 SOLWEIG
   합계 23시간 51분(2026-07-22 02:45:51~2026-07-23 02:37:35) + 모자이크 42초 +
   UTCI 77초 = **총 85,999초(23시간 53분 19초)**. 참고로 이전 무타일 단일 실행
   시도는 SVF만 별도로 약 40시간이 걸렸고 SOLWEIG 단계에서 OOM으로 실패했음(별도
   참고용 수치, 아래 그래프에는 미포함).

## 3. 결과 — 소요시간

| 해상도 | 다운샘플 | Wall H/A | SVF | SOLWEIG | UTCI | **총 소요시간** |
|---|---|---|---|---|---|---|
| 1m(9분할) | - | - | - | 85,860초 | 77초 | **85,999초(23h 53m)** |
| 5m | 1초 | 77초 | 526초 | 492초 | 4초 | **1,100초(18m 20s)** |
| 10m | 2초 | 48초 | 63초 | 105초 | 13초 | **231초(3m 51s)** |
| 30m | 1초 | 6초 | 5초 | 10초 | 1초 | **23초** |

1m→5m로 해상도를 낮추는 것만으로 전체 소요시간이 **약 78배** 줄어든다(85,999초→1,100초).
5m→10m는 추가로 약 4.8배, 10m→30m는 추가로 약 10배 단축된다 — 로그-로그 스케일에서
거의 직선(멱함수 관계)에 가깝다.

## 4. 결과 — 정밀도(1mtrue 기준)

| 해상도 | 픽셀 Tmrt r | 픽셀 UTCI r | 픽셀 HardCut38 일치 | 링크 Tmrt r | 링크 UTCI r | 링크 HardCut38 일치 |
|---|---|---|---|---|---|---|
| 1m(기준) | 1.000 | 1.000 | 100.00% | 1.000 | 1.000 | 100.00% |
| 5m | 0.945 | 0.945 | 97.96% | 0.939 | 0.939 | **98.87%** |
| 10m | 0.877 | 0.877 | 98.26% | 0.819 | 0.819 | 98.02% |
| 30m | 0.681 | 0.681 | 98.39% | 0.561 | 0.559 | 97.69% |

## 5. ⚠️ 정직한 관찰 — "5m이 엘보우 포인트"라는 결론은 지지되지 않음

그래프(`03_Method_C/results/figures/2026-07-29_resolution_tradeoff_speed_vs_precision.png`)를
있는 그대로 보면:

- **Hard Cut(UTCI≥38°C) 일치율은 1m~30m 전 구간에서 97.7~100%로 거의 평평하다** —
  뚜렷한 엘보우가 보이지 않는다. 오히려 30m에서도 97.69%로 여전히 높다.
- **UTCI/Tmrt 상관계수(r)는 완만하게 감소하다가 10m→30m 구간에서 뚜렷이 급락한다**
  (링크 r: 5m 0.939 → 10m 0.819 → 30m 0.559). 이 기준으로 보면 엘보우는 5m이 아니라
  **10m과 30m 사이**에 있다고 보는 게 데이터에 더 충실한 서술이다.
- 5m과 10m의 정밀도 차이(r 0.939 vs 0.819, HardCut38 98.87% vs 98.02%)는 크지 않다 —
  즉 이 데이터만 놓고 보면 "10m도 충분히 정밀했을 것"이라는 반론도 가능하다.

**결론을 억지로 5m에 맞추지 않는다.** 대신 아래처럼 정직하게 정리한다: (1) Hard Cut
지표 자체는 해상도에 매우 강건해 이 지표만으로는 해상도 선택을 정당화하기 어렵다,
(2) 연속형 지표(r) 기준으로는 5m이 "정밀도 손실이 아직 완만한 안전한 구간"에 있고
"급격한 정밀도 손실이 시작되는 10-30m 구간"에는 못 미친다는 점에서 보수적으로
타당한 선택이다, (3) 소요시간 기준으로는 5m이 1m 대비 78배 빠르면서도 정밀도 손실은
5.5%(r 기준)에 불과해 비용 대비 효율이 가장 좋은 지점 중 하나다.

## 6. 생성된 파일

**10m 전체 시각화 세트(11개, 1mtrue와 동일 포맷)**: `03_Method_C/results/figures/
seongdong_10m_standalone/`
**5m/30m 평균맵만**: `03_Method_C/results/figures/seongdong_5m_clean_standalone/`,
`seongdong_30m_clean_standalone/`
**트레이드오프 그래프**: `03_Method_C/results/figures/
2026-07-29_resolution_tradeoff_speed_vs_precision.png`
**원자료**: `03_Method_C/results/compare_resolution_tradeoff/
2026-07-29_timing_summary.csv`, `2026-07-29_precision_vs_1mtrue.csv`,
`2026-07-29_tradeoff_merged.csv`
**링크 데이터**: `03_Method_C/results/2026-07-29_link_tmrt_utci_seongdong_10m.gpkg`
**코드**: `downsample_seongdong_1mtrue.py`, `compute_utci_seongdong_clean.py`(서버),
`compute_precision_vs_1mtrue.py`, `plot_resolution_tradeoff.py`,
`plot_seongdong_10m_standalone.py`, `plot_seongdong_10m_link_maps.py`,
`plot_seongdong_clean_avgmaps.py`, `run_seongdong_clean_pipeline.sh`/
`run_seongdong_clean_chain.sh`(서버)

> 참고: `03_Method_C/results/figures/2026-07-29_hardcut_vs_continuous_normalization.png`
> 파일이 작업 폴더에 있으나 이 작업으로 생성한 파일이 아님(생성 시각이 이 작업 착수
> 이전) — 다른 경로로 만들어진 것으로 보이니 확인 필요.

## 7. 논문 내 배치

`writing/03_논문구조/2026-07-17_논문전체구조_v3.md` 기준:

- **1차 배치: 4.1절("MRT 산출 — SOLWEIG 단일 파이프라인") 말미** — 현재 "DEM은
  Copernicus GLO-30(30m 오픈소스)을 5m 계산격자로 리샘플링하며..." 문장 바로 뒤에,
  "성동구를 대상으로 1/5/10/30m 해상도별 소요시간·정밀도(1m 기준)를 비교한 결과,
  5m은 1m 대비 계산량을 약 78배 절감하면서도 링크 단위 Hard Cut 판정 일치율
  98.87%를 유지한다(부록 X 또는 그림 Y 참고)"는 문장을 추가해 5m 채택의 실증
  근거로 제시.
- **2차 배치(교차 언급): 6.4절("SOLWEIG의 도시 규모 확장 — 방법론적 기여")** —
  이 절이 이미 "타일링이 결과를 왜곡하지 않음"을 다루고 있으므로, 그 바로 뒤에
  "격자 해상도 선택 역시 같은 논리로 검증했다(4.1절, 부록 X)"는 한 문장으로
  연결해 "SOLWEIG를 도시 규모로 확장하기 위한 두 가지 실용적 결정(타일링·해상도)
  모두 실증적으로 검증됐다"는 하나의 통합된 방법론적 기여로 묶을 수 있음.
- 그래프 자체는 부록(Appendix) 또는 4.1절의 그림으로 삽입 권장 — 본문에 넣기엔
  다소 방법론적/보충적 성격이 강함.

## 8. 방어 논리(디펜스)

리뷰어가 "왜 5m을 골랐는가"라고 물으면 다음과 같이 답할 수 있다:

> "성동구를 대상으로 1/5/10/30m 해상도에서 동일한 지형·건물·수목 데이터로 SOLWEIG를
> 실행해 비교한 결과, Hard Cut(UTCI≥38°C) 판정의 링크 단위 일치율은 전 해상도에서
> 97.7% 이상으로 안정적이었고, 연속형 지표(Tmrt/UTCI 상관계수)의 정밀도 손실은
> 10m를 넘어 30m로 갈 때 급격히 커졌다. 5m은 이 급격한 손실 구간에 진입하기 전
> 안전한 지점에 있으면서도, 1m 대비 계산 비용을 약 78배 절감해 서울 전역(605.70km²)
> 규모 적용을 현실적으로 가능하게 한다. 즉 5m은 '엘보우 포인트 그 자체'는 아니지만,
> '정밀도 손실이 아직 완만한 구간에서 계산 비용을 최대한 아끼는 지점'이라는 근거로
> 선택했다."

이 논리는 서울 전역 v2-v3 비교(GLO-30 30m 소스 vs 진짜 1m 소스, 5m 계산격자 고정,
r=0.9964·Hard Cut 일치 99.84%)로 이미 검증된 5m 자체의 결과 신뢰성과 결합하면 더
강해진다 — "5m 계산격자 자체의 정확도"(v2-v3)와 "5m이라는 해상도 선택의 비용
효율성"(이 문서)이 서로 다른 두 축에서 5m을 뒷받침한다.

## 9. 보강된 방어 논리 — "매끄러운 엘보우" 대신 "실행가능선(feasibility threshold)"

사용자(신진) 재해석(2026-07-29): 5절의 정직한 관찰("매끄러운 엘보우는 5m이 아니다")은
그대로 유효하지만, **애초에 "엘보우"를 매끄러운 곡선의 변곡점으로 찾을 필요가 없다**는
반론이 가능하다. 대신 "실행 가능/불가능"이라는 이분법적 문턱(threshold)으로 보면
훨씬 단순하고 강한 논리가 된다:

### 9-1. 논리 구조

1. 성동구 연산 도메인(29.16km², 직접 계산) 기준 1m은 23시간 53분 소요. 서울 전체
   (605.70km², 이미 확정된 연구지역 정의)로 **면적비(20.77배) 선형 추정**하면 약
   **496시간(약 20.7일)** — SCI 투고·논문 제출 타임라인상 명백히 비현실적(⚠️ 이는
   거친 선형 추정으로, 실제로는 타일 수 증가에 따른 고정비용 중복 등으로 더 나빠질
   가능성이 높음 — 참고용 수치로만 제시할 것).
2. 반면 5m(약 6.3시간)·10m(약 1.3시간)·30m(약 8분)은 셋 다 **이미 "실행 가능한
   영역"** 안에 있다 — 게다가 5m은 **추정이 아니라 서울 전역 v2/v3 파이프라인으로
   이미 실제로 완주해 메인 분석에 쓰고 있는, 실증된 사실**이다.
3. "실행 가능한 영역"(5/10/30m) 안에서는 굳이 정밀도를 더 희생할 이유가 없다 —
   정밀도(링크 Hard Cut 일치율)는 5m(98.87%) > 10m(98.02%) > 30m(97.69%)로
   **일관되게 5m이 가장 높다.**
4. 따라서: **진짜 갈림길은 "5m이냐 10m이냐"가 아니라 "1m이냐, 그 아래(5·10·30m)
   전부냐"이며, 일단 실행 가능한 범위에 들어오면 그중 가장 정밀한 5m을 택하는 것이
   당연하다.**

### 9-2. 그래프

`03_Method_C/results/figures/2026-07-29_resolution_feasibility_threshold.png`
(코드: `plot_resolution_feasibility_threshold.py`) — 왼쪽: 해상도별 서울 전역
추정 소요시간(로그스케일, 1m만 빨강으로 강조해 "실행 불가 영역"임을 시각화).
오른쪽: "실행 가능한" 5/10/30m 중 정밀도 비교(5m이 가장 높음을 강조).

### 9-3. 5절 관찰과의 관계

5절의 발견("연속형 지표 r은 10-30m 구간에서 급락, 5m이 매끄러운 곡선의 엘보우는
아님")은 여전히 정직하게 유지한다. 다만 9-1의 논리는 그 발견과 **모순되지 않고
보완**한다 — "곡선의 기울기가 완만해지는 지점"과 "실행 가능/불가능의 경계"는
서로 다른 질문이며, 후자가 5m 채택을 정당화하는 데 더 직접적이고 강한 근거를
제공한다. 지도교수님께 보고 시 두 관점을 함께 제시하는 것을 권장:
"매끄러운 엘보우 자체는 5m이 아니라 10-30m 사이에 있지만, 실행 가능 여부라는
기준으로 보면 5m이 명확히 정당화된다."
